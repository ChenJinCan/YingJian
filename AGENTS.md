# 映见应用 Agent 规则

## 产品目标

映见是一款以本地图像处理引擎为核心的智能修图应用。核心承诺是“一张精修，整组好看”。

优先级固定为：本地修图质量、整组一致性、智能配方、可选生成式 AI。默认三套推荐不得创建云端图片生成任务。

## 工程边界

- Flutter 负责 UI、交互、项目状态、编辑配方和任务编排。
- 高频像素处理、预览纹理和高清导出位于 iOS/Android 原生能力边界之后。
- Flutter 与原生层传递路径、参数和状态，不在滑块交互中往返传输完整图片字节。
- 原图只读；编辑必须非破坏、可撤销、可恢复，并从原图高清导出。
- 商业人像 SDK 和云端模型必须封装在供应商无关接口之后。

## Runtime 安全

- 不得在生产路径无保护地使用 debug-only、test-only 或 assert-only API。
- Flutter debug-only framework member 必须以短路的 `kDebugMode && ...` 保护，不能用 `kReleaseMode` 代替。
- 设备敏感路径变更后运行窄回归测试，并在受影响平台做 Profile 或 Release 真机冒烟。
- iOS 渲染、分享、插件、签名或生命周期差异需要物理设备证据才能宣称完成。

## 图像与隐私

- 默认本地处理不上传原图。
- 云端语义编辑只上传用户明确选择的照片和必要区域。
- 不把图片、签名 URL、密钥、完整个人提示或人脸标识写入普通日志。
- 生成式 AI 失败不能破坏本地结果；失败、取消或质量门拒绝不得错误扣权益。

## 测试和完成标准

- 测试公开行为接缝，不耦合内部 Widget、Shader 或供应商对象。
- 图像处理使用固定样片和明确容差；主观人像质量需要人工盲评补充。
- Debug 或模拟器结果不能代替 Profile/Release 真机性能证据。
- 变更完成前搜索新增生产源码中的 debug-only API。

## Git 与发布

- 保持提交范围单一，不提交密钥、签名材料、构建产物或临时图片。
- 本地构建、上传、provider processing、provider valid、测试组分发、真实测试者可达、审核和公开可用必须分别报告。
- 未获明确授权不得上传 TestFlight/Google Play、提交审核或公开发布。
- 正式候选必须从干净、已推送且与远程同步的 release worktree 构建，并满足当前 release contract。
