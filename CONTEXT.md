# 映见领域上下文

本文件只维护跨产品、设计和工程共同使用的稳定术语。产品依据见 `docs/product/`，具体实现合同见 MVP Spec 和质量基线。

## 产品术语

### 照片项目

一次本地编辑任务，只包含 1 张只读原图引用、项目状态、编辑配方、历史和导出状态。多个照片项目可以同时保存在本地，彼此独立。

### 项目草稿

已保存、可从首页继续的单图照片项目。应用允许多个项目草稿同时存在；新建草稿不覆盖或删除已有草稿，删除只作用于用户明确选择的草稿。

### 元操作（MetaOp）

最小编辑能力，例如曝光、裁剪或对指定人物瘦脸。每个元操作都有稳定名称、参数范围、中性值和作用目标，但不代表一次完整用户操作。

### 事务（EditTransaction）

用户一次意图产生的一组元操作变化，也是预览、应用、撤销和重做的完整单位。事务内任一变化无效时，整次操作都不生效。

### 编辑状态（EditState）

当前照片或项目已经应用的全部编辑结果。它可以从只读原图重放，并同时用于预览、导出和项目恢复，不是缓存图片。

### 操作地址（OpAddress）

在编辑状态中唯一定位一个值，由元操作、照片、范围、目标和参数组成。同一事务内一个地址最多修改一次。

### 编辑目标

某项编辑明确作用的照片、人物、身体主体、背景或局部区域。目标无法可靠恢复时，相关元操作暂停并等待用户重新选择，不自动转移。

### 状态版本（StateRevision）

每次事务成功后生成的编辑状态版本。AI 提案必须绑定它；版本已变化的旧提案不能覆盖新编辑。

### 元操作目录（MetaOpCatalog）

全部合法元操作的唯一清单，统一声明参数、目标、UI 表现、AI 可用条件和平台要求。任何入口都不能创建目录外效果。

### 草稿（EditDraft）

手势未结束或 AI 尚未返回时的临时事务。校验和渲染成功后自动提交；失败或离开当前上下文时取消。

### 渲染计划（RenderPlan）

由编辑状态确定生成、交给本地图像引擎的有序计划。预览和导出使用同一计划语义，顺序不由 AI 或 UI 决定。

### 平台能力（PlatformCapabilities）

当前设备对每个元操作版本的预览、导出、目标数量和资源限制。能力不足时明确拒绝，不能静默省略。

### 检查点（Checkpoint）

把较早历史合并成稳定起点，只压缩历史，不改变当前图片结果。

### 编辑资源

编辑状态引用的应用自有数据，例如背景图片或蒙版。当前状态、历史或草稿仍引用时必须保留，全部引用消失后才能回收。

### AI 提案（AIProposal）

AI 根据用户意图和可用元操作提出的结构化事务草稿。提案没有执行权；统一校验和渲染成功后自动成为可撤销事务。

### AI 规划器（AIPlanner）

把自然语言转换为 AI 提案的可替换能力。它只能使用授权元操作，不能生成渲染计划或直接修改图片。

### 历史（EditHistory）

按提交顺序保存的事务，用于解释、撤销、重做和恢复。来源只标记手动、推荐或 AI，不形成独立效果层。

### 对比基准

前后对比使用的起点。全局对比以原图为准，本次修改对比以事务前的编辑状态为准。

### 共享风格

整组共同的审美属性，例如色彩倾向、对比关系、滤镜质感和颗粒。

### 逐张补偿

为保持同组风格而对每张照片独立计算的修正，例如曝光、白平衡、肤色保护和安全强度。

### 单张覆盖

用户只针对当前照片做出的修改。它叠加在共享风格和逐张补偿之上，不会默认影响其他照片。

### 推荐方案

经过人工调校的基础配方家族，加上根据当前输入计算的安全强度和逐张补偿。三套方案是本地参数结果，不是三张生成图片。

### 视觉语义轨道

把一个明确的视觉方向表示为可连续探索的有界路径；轨道位置只产生经过校验的编辑草稿，不能成为绕过元操作目录的隐藏效果层。

_避免使用_：视觉时空轴、魔法滑杆、AI 滤镜。

### 光照轨道

让用户以虚拟光源的位置和光质直接表达重布光意图的专用视觉轨道；它描述目标光照，不声称恢复拍摄现场的真实光源。

_避免使用_：左右滤镜、真实光线恢复。

### AI 重塑

用户主动选择、会改变原图中对象结构或场景内容的生成式编辑，例如年龄风格或陆地与水域转换。它独立于默认本地闭环，必须明确成本、隐私、取消和结果不确定性。

_避免使用_：基础修图、实时滤镜、AI 自动美化。

### 单张精修

在可靠的构图、光色、滤镜与画质改善上，对重点照片进行可控调整。MVP 人像精修包含自然质感、逐脸基础五官与轮廓、逐主体基础身体塑形和背景保护；只有入口、检测、全局调色或单一磨皮都不构成单张精修。

### 竞品入场能力

用户在映见内完成与直接质量基准相同的核心修图任务，并得到可替代的自然结果。它按同图结果验收，不按工具名称、入口数量或底层技术相似度验收。

### 一键自然美化

根据当前人像的实际问题组合安全的质感磨皮、肤色与面部光线、瑕疵减弱，直接提供自然的第一结果。它可由用户选择推荐方案时带入，并展开为真实子参数；不是额外隐藏效果，也不自动启用瘦脸或瘦身。

_避免使用_：用“自然精修”同时指代一键结果、磨皮算法和全部人像能力。

### 质感磨皮

减弱皮肤区域的局部粗糙和不均匀，同时保留毛孔、轮廓、五官与身份特征的独立人像能力。

### 肤色与面部光线

改善皮肤色偏、明暗不均和面部阴影，同时保持人物原有肤色身份与场景光线关系的独立人像能力。

### 瑕疵减弱

自动识别并减弱痘印、色斑等离散皮肤瑕疵，同时避免删除痣、雀斑、皱纹、妆容和身份特征的独立人像能力。

### 人像适用范围

一键自然美化及其三个非几何子能力可分别处理最多三张高置信度人脸；面部塑形支持在最多三张可用人脸中逐脸选择并保存独立参数，身体塑形在多人照片中先由用户选择可用主体。低置信度对象独立关闭，不阻断其他人物、基础修图或导出；应用不执行年龄或性别推断。

### 背景保护

几何塑形时结合人物/身体蒙版、关键点约束和背景结构，尽量把位移限制在目标人物内部并保持非目标人物与背景稳定。工具有变化不等于通过背景保护质量门。

### 局部蒙版

描述局部工具作用区域的版本化、可编辑数据。空蒙版严格无效果；蒙版可以显示、擦除、恢复、撤销和随项目保存，不保存重复的完整原图。

### 同图竞品门

映见与直接质量基准使用相同输入、相同修图任务和匿名结果逐项比较的质量门。每项能力独立过门，其他能力的高分不能抵消假面、身份变化、五官漂移或背景变形。

### 整组一致性

同组照片共享审美方向，同时根据每张输入独立修正。它不等于把同一组绝对参数复制到所有照片。

### 原像素尺寸高质量导出

从应用持有的原始输入重新执行有效配方，并按裁剪结果保留原始像素尺度的高质量输出。它不是预览截图，也不表示有损 JPEG 与原文件逐字节相同。

### 画质改善

针对噪点、暗部不可读、低对比灰雾和轻微软化的确定性本地修复。它由去噪、暗光提亮、去灰和细节锐化四个可独立归零的真实参数组成；一键入口只写入安全推荐值，不形成隐藏效果，也不承诺恢复原图中不存在的细节。

### 可编辑主体蒙版

由端侧人物分割生成的主体起始蒙版，以及用户以添加或擦除画笔做出的版本化修正。修正以归一化坐标保存，可预览、撤销、恢复并从原图重放；自动分割结果或背景效果参数本身不等同于可编辑蒙版。

### 局部光色蒙版

用户独立绘制的局部作用区域，承载可归零的曝光与饱和度参数。它不复用消除笔，也不上传照片；空蒙版严格无效果。

### 图像质量基准集

来源和使用权明确、清单与哈希固定的照片集合，用于跨平台渲染、人像自然度、整组一致性和回归验证。未经授权的真实用户照片不得进入。

### 质量门

用于判断对应工程阶段或冻结候选是否可继续的自动检查、人工盲评或物理设备预算。功能入口存在不等于通过质量门；最终真机质量门不阻断功能实现，但会阻断候选验收。

## 状态术语

- **功能实现完成**：一个可观察功能切片的入口、操作、结果、异常状态、持久化与窄测试已经闭环；不代表已经通过模拟器或真机验收。
- **模拟器验证通过**：完整功能切片已通过生产导航、核心交互、集成测试和自动工程检查；不代表已取得真机性能、色彩或系统能力证据。
- **候选已冻结**：源码、版本、build、配置和产物身份已固定，可以开始绑定最终真机与人工证据。
- **真机验收通过**：冻结候选已通过要求的 Profile/Release 或 TestFlight 主旅程、设备矩阵和人工质量门。
- **已冻结**：当前实施必须遵守，修改需要同步更新相关规格和依据。
- **待验证**：已有目标或假设，但缺少样片、用户、Profile/Release 或物理设备证据。
- **数据缺失**：没有足够事实支撑结论，不能按零或通过处理。
- **阻断**：对应门禁未满足，后续受影响阶段不得宣称完成。
