# 映见领域上下文

本文件只维护跨产品、设计和工程共同使用的稳定术语。产品依据见 `docs/product/`，具体实现合同见 MVP Spec 和质量基线。

## 产品术语

### 照片项目

一次本地编辑任务，包含 1–6 张只读原图引用、项目状态、共享风格、逐张补偿、单张覆盖和导出状态。

### 编辑配方

可序列化、可撤销、可复现的一组确定性编辑参数。配方描述如何从原图得到结果，不保存重复的完整位图。

### 共享风格

整组共同的审美属性，例如色彩倾向、对比关系、滤镜质感和颗粒。

### 逐张补偿

为保持同组风格而对每张照片独立计算的修正，例如曝光、白平衡、肤色保护和安全强度。

### 单张覆盖

用户只针对当前照片做出的修改。它叠加在共享风格和逐张补偿之上，不会默认影响其他照片。

### 推荐方案

经过人工调校的基础配方家族，加上根据当前输入计算的安全强度和逐张补偿。三套方案是本地参数结果，不是三张生成图片。

### 单张精修

在可靠的基础光色与构图上，对重点照片进行可控调整。人像精修至少包含三个独立语义：改善肤色/面部光线/皮肤纹理的 `自然精修`，保护五官与背景的克制 `瘦脸`，以及保护头部/四肢/背景的克制 `瘦身`。只有色彩滑块、只有人像检测，或只有磨皮，都不构成映见 MVP 的单张精修。

### 整组一致性

同组照片共享审美方向，同时根据每张输入独立修正。它不等于把同一组绝对参数复制到所有照片。

### 原像素尺寸高质量导出

从应用持有的原始输入重新执行有效配方，并按裁剪结果保留原始像素尺度的高质量输出。它不是预览截图，也不表示有损 JPEG 与原文件逐字节相同。

### 图像质量基准集

来源和使用权明确、清单与哈希固定的照片集合，用于跨平台渲染、人像自然度、整组一致性和回归验证。未经授权的真实用户照片不得进入。

### 质量门

在继续实现或宣称完成前必须通过的自动检查、人工盲评或物理设备预算。功能入口存在不等于通过质量门。

## 状态术语

- **已冻结**：当前实施必须遵守，修改需要同步更新相关规格和依据。
- **待验证**：已有目标或假设，但缺少样片、用户、Profile/Release 或物理设备证据。
- **数据缺失**：没有足够事实支撑结论，不能按零或通过处理。
- **阻断**：对应门禁未满足，后续受影响阶段不得宣称完成。
