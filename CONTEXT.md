# 映见创作

映见帮助用户从一张照片出发，快速确定想要的视觉方向，并得到可信的静态或动态作品。本上下文只定义用户与产品共同使用的领域语言，不收录界面控件、内部执行方式或阶段性实现状态。

## Language

**源照片**:
用户明确选入映见、作为本次创作起点的照片。源照片始终保持原样，所有预览、结果和导出都能够追溯到它。
_Avoid_: 待修图、素材图、工作副本

**创作意图**:
用户希望作品呈现的感觉、故事或用途，可以通过选择、文字或语音表达。创作意图描述想达到什么效果，而不是怎样操作工具。
_Avoid_: 操作指令、工具要求、AI 提示词

**创作任务**:
用户在首页首先选择的结果方向：**优化照片**、**换风格**、**去背景 / 去杂物**或**做动态效果**。创作任务在选图前确定，并约束后续页面和结果类型；切换任务意味着返回首页重新选择。前三项的目标结果为静态作品，做动态效果的目标结果为动态作品。选择任务不等于选择或授权任何具体处理能力。
_Avoid_: 抽象的“图片应用”总入口、定风格后的二选一、同屏结果按钮、隐式任务切换

**创作能力**:
用户在已选创作任务内，通过名称明确的选项主动选择的一项结果能力。一次选择只授权这一项能力；产品不得替用户推荐、排序、组合、替换、隐式选中或执行其他能力。能力不可用或失败时保留用户选择和当前安全状态，由用户决定下一步。
_Avoid_: 自动模式、智能推荐、默认能力、能力组合、静默回退

**任务执行分支**:
把用户任务映射到静态页面或动态页面的内部路由身份。`CreationTask=optimize|style|cleanup|motion` 保存用户选中的任务；`CreationIntent=apply|motion` 仅区分静态结果与动态结果。`optimize`、`style`、`cleanup` 都映射到 `apply`，`motion` 映射到 `motion`。旧 `apply` 草稿迁移为 `style`，不重写原有结果。执行分支不表示用户选择了哪项创作能力，也不构成上传、生成或费用授权。
_Avoid_: 用执行分支替代用户可见任务、把旧草稿随机归类、把静态分支等同于本地执行

**创作草稿**:
围绕一张源照片持续保存任务、用户选择的能力、必要输入和结果的创作空间。用户可以离开后继续当前任务，也可以保留多个不同方向的结果。
_Avoid_: 编辑项目、临时会话、结果文件

**风格**:
用户能够辨认和选择的一种整体视觉方向，例如自然、通透、电影感或梦幻。风格表达作品看起来和感受起来如何，而不是一组工具名称。
_Avoid_: 滤镜、工具集合、效果开关

**风格定义**:
对目标风格清晰、可理解且可复用的描述，它说明画面气质与视觉重点。定义只属于用户已经选择的换风格能力，并只推动该能力继续。
_Avoid_: 参数预设、操作步骤、模糊愿望

**风格参考**:
用户用来表达目标感觉的示例画面，它只帮助形成风格定义，不会取代源照片或被直接当作结果。参考中的人物、物体和身份内容不属于待复制的风格。
_Avoid_: 源照片、生成素材、照搬对象

**风格候选**:
用户主动打开固定候选列表后可以明确点选的一个方向，用于比较、切换或继续完善。候选不根据照片或历史自动生成、排序或突出；查看候选本身不会自动应用、生成或导出作品。
_Avoid_: 最终结果、自动决定、隐藏任务

**定风格**:
用户在“换风格”任务中先明确选择官方、文字、语音、参考图或 AI 风格重绘，再把创作意图收敛为明确风格定义的过程。风格清楚后，用户只执行当前能力的唯一主操作。
_Avoid_: 修工具、选滤镜、配置流程

**风格切换**:
在保留同一源照片的前提下，把当前风格定义换成另一个方向。切换会更新接下来看到的预览或结果，但不得在未确认时启动生成。
_Avoid_: 覆盖源照片、自动出片、重新导入

**AI 风格助手**:
用户主动选择文字定风格、语音定风格或参考图风格后，把用户已经确认的输入转换成受约束风格定义的协作角色。它不得分析照片后提出、排列或替用户选择风格。使用它不等同于上传照片，也不等同于生成作品。
_Avoid_: AI 滤镜、看图推荐、自动替用户决定、生成器

**一键完成**:
用户用一次明确动作执行当前已经选择的创作能力，不再经过额外的通用工具配置。它承诺路径直接，不表示系统可以默认选择、组合或替换能力，也不承诺跳过隐私、成本或生成确认。
_Avoid_: 全自动发布、无确认扣费、连续配置

**应用**:
把已确定的本地静态操作稳定地作用于源照片，得到可预期、可重现的视觉结果。它可以是风格、质量优化、语义背景处理或局部擦除；应用不改变源照片，也不主动创造与源照片无关的新内容。
_Avoid_: 生成、导出、覆盖原照片

**生成**:
用户明确发起、用于创造新画面内容或时间变化的创作动作，其精确结果无法在开始前完全确定。生成可以产出静态作品或动态作品，并与应用保持清晰区分。
_Avoid_: 应用、后台自动处理、浏览候选

**预览**:
帮助用户在完成前判断某个风格候选、应用或生成方向的临时可视结果。预览不是已保留的结果，也不是已导出的媒体。
_Avoid_: 结果、导出文件、源照片

**结果**:
由一次完成的应用或生成产生、保留在创作草稿中的完整产物。结果可以继续比较、重新选择当前任务能力或导出，但始终与源照片相互独立。
_Avoid_: 预览、源照片、外部文件

**静态作品**:
以单一静止画面表达风格的结果。它可以来自应用或生成。
_Avoid_: 源照片、预览缩略图、动态作品

**动态作品**:
通过随时间发生的运动或变化，让源照片呈现“动起来”效果的结果。它是用户可保留和导出的作品，不是界面动画、加载反馈或静态效果。
_Avoid_: 动效按钮、处理中动画、动态预览占位

**导出**:
把用户选定的结果制作为可在映见之外保存或分享的媒体。导出不会改变源照片，也不代表已经公开发布。
_Avoid_: 应用、生成、自动发布
